Rename ABAP2XLS to allow multiple version of the functionality (as standard, naming convention ZEXCEL is used).

Adjust file __.github/workflows/action.yml__ to set the new naming convention for ABAP2XLS. 
